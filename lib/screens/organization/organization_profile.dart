import 'package:flutter/material.dart';

class OrganizationProfile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Organization Profile")),
      body: Center(child: Text("Organization Profile Details Here")),
    );
  }
}
